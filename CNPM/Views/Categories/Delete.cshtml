@model CNPM.Models.Category

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Delete</title>
</head>
<body>
    <h2 class="text-center">Xóa loại</h2>

    <h3 class="text-danger">Bạn có chắc chắn là bạn muốn xóa loại sách này?</h3>
    <div>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                Tên loại
            </dt>

            <dd>
                @Html.DisplayFor(model => model.name)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Xóa" class="btn btn-danger" /> |
                @Html.ActionLink("Quay về danh sách", "Index", null, new { @class = "btn btn-secondary" })
            </div>
        }
        @if (!string.IsNullOrEmpty(ViewBag.ErrorCate))
        {
            <div class="alert alert-danger">
                @ViewBag.ErrorCate
            </div>
        }
    </div>
</body>
</html>
